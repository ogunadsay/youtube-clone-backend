version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm start

artifacts:
  files:
    - '**/*'
  exclude:
    - node_modules/
    - .git/
    - .gitignore
    - buildspec.yml
