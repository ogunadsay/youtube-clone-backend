version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  pre-build:
      - npm install
  build:
    commands:
      - npm start

artifacts:
  files:
    - '**/*'
  exclude:
    - node_modules/
    - .git/
    - .gitignore
    - buildspec.yml
